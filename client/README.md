# CareerPath React Frontend

This is the React + Vite frontend for CareerPath. It is configured for local development and proxies API calls to the Laravel backend at `http://127.0.0.1:8000`.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:5173`. Start the Laravel backend on port `8000` before using API-backed features.
